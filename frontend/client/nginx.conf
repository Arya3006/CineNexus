server {
    listen 5173;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Handle React Router client-side routing
    location / {
        try_files $uri /index.html;
    }
}